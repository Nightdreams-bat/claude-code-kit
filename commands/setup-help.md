---
description: Guide me through a manual setup step by step, always showing the steps still remaining. Usage: /setup-help <what you're setting up>
---

The argument is: `$ARGUMENTS`

Load the `setup-help` skill and guide the user through `$ARGUMENTS`.

- Do not perform the steps for them — they run it; you explain.
- One step at a time. End every reply with the "Done so far" / "Still to do" lists.
- If `$ARGUMENTS` is vague, ask the blocking questions first (host, OS, account status).
